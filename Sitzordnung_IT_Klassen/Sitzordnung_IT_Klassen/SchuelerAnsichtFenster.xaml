<Window x:Class="Sitzordnung_IT_Klassen.SchuelerAnsichtFenster"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sitzordnung_IT_Klassen"
        mc:Ignorable="d"
        Title="SchuelerAnsichtFenster" Height="450" Width="800">
    <Grid>
        <DataGrid Name="datagrid1" HorizontalAlignment="Left" Height="316" Margin="62,49,0,0" VerticalAlignment="Top" Width="608">
            <DataGrid.Columns>
            <DataGridTextColumn Header="Name" Binding="{Binding Name}"></DataGridTextColumn>
            <DataGridTextColumn Header="Vorname" Binding="{Binding FirstName}"></DataGridTextColumn>
            <DataGridTextColumn Header="Beruf" Binding="{Binding Work}"></DataGridTextColumn>
            <DataGridTextColumn Header="Betrieb" Binding="{Binding CompanyName}"></DataGridTextColumn>
            <DataGridTextColumn Header="Geschlecht" Binding="{Binding Gender}"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Button Content="Öffnen" HorizontalAlignment="Left" Margin="687,78,0,0" VerticalAlignment="Top" Width="75"/>
        <Button Content="Speichern" HorizontalAlignment="Left" Margin="687,121,0,0" VerticalAlignment="Top" Width="75"/>
    </Grid>
</Window>
